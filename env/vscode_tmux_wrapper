#!/usr/bin/env bash
set -euo pipefail

if tmux list-sessions 2>/dev/null | grep window &>/dev/null
then
    export "`tmux showenv PATH`"
    if [[ ! "$OSTYPE" =~ ^darwin ]];
    then
        export "`tmux showenv VSCODE_IPC_HOOK_CLI`"
    fi
fi

code "$@" &>/dev/null || mcedit "$@"
