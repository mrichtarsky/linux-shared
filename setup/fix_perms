#!/usr/bin/env bash
set -euxo pipefail

REPO_PATH=/repos
chown -R root:linux-shared "$REPO_PATH/linux-shared" "$REPO_PATH/secrets"
chmod -R o-rwx,g=u "$REPO_PATH/linux-shared" "$REPO_PATH/secrets"
chmod a+r "$REPO_PATH/linux-shared/setup/_impl/sitecustomize.py"
